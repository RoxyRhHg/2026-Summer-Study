# 2026 暑假学习笔记协作规范

## 适用范围

- 本文件适用于 `基础知识/2026暑假学习/` 及其全部子目录。
- 本文件是该学习仓库的**唯一总控规则入口**；子目录原则上不再通过重复 `AGENTS.md` 强化通用规则。
- 其他 Agent 接手时，应优先读取本文件，不应只依赖历史聊天印象。
- `.agent-guides/` 保存详细执行与写作规范；**何时触发、必须读什么、什么时候允许开始正文**始终由本文件决定。
- 用户当前明确指令优先于本文件；但不得把“未读取规则 / 未调用必要工具”描述成已完成。

---

# 零、最高优先级：任务路由与执行门禁

> [!danger] 先判断任务，再开始正文
> 每个学习任务开始前，先判断是否属于长篇资料任务，以及本次要生成 / 修改哪类笔记。命中对应任务后，必须完成本节规定的强制动作和规范读取；未完成前不得直接进入正文写作。

## 0.1 长篇资料自动触发

满足下列任一条件，即自动进入`长篇学习工作流`：

- 研究或整理整本教材、完整课程或较长讲义；
- 任务明确跨越两个及以上章节；
- 用户要求系统完善、补全或重构一门课程 / 一本教材的笔记；
- 需要持续进行分章深读、分章归纳或跨章综合；
- 研究多章节教程、课程式论文材料或其他长篇学习资料。

以下任务本身不触发：

- 只回答一个明确的局部知识问题；
- 只修改工作流、模板、`AGENTS.md` 或规范文件，且不进入教材正文；
- 用户明确要求本次不得使用子 Agent。

## 0.2 长篇任务必须实际创建子 Agent

一旦触发：

1. 主 Agent 只先做**最小必要结构扫描**：目录、章节边界、资料对应关系、处理顺序。
2. 进入任何章节正文深读、修改或写作前，必须实际创建当前章节子 Agent。
3. 必须读取 `.agent-guides/长篇学习工作流.md`。
4. 再根据当前产物读取对应写作规范。

`已触发子 Agent`的唯一成功判据是同时满足：

- 已实际调用子 Agent；
- 已获得可识别的子 Agent 名称、任务名或标识；
- 已明确该子 Agent 负责的章节或独立任务边界。

以下都**不算**触发成功：

- 只说“将使用子 Agent”；
- 只建立分章计划；
- 只采用章节隔离阅读；
- 主 Agent 自己完成全部章节工作后再声称遵循了规则。

## 0.3 固定执行顺序

`主 Agent 扫描目录与章节边界 → 创建当前章节子 Agent → 子 Agent 完成章节闭环 → 主 Agent 验收 → 再调度下一章`

- 默认按章节依赖顺序一次处理一章，不因使用子 Agent 就盲目并行。
- 主 Agent 负责全书地图、调度、验收和跨章候选关系，不代替章节子 Agent 深写正文。
- 章节子 Agent 负责当前章的`原文深读 → 知识点型笔记 → 讲解版 → 思维导图 → 必要碎片问答`闭环。
- 全部章节分别完成并验收后，才进入真正的全书综合与跨章双链审查。
- 用户明确指定为`只读 / 不改 / 仅作基准`的章节或文件，任何 Agent 都不得修改。

## 0.4 子 Agent 失败时禁止静默退化

- 子 Agent 工具不可用、创建失败或并发槽位不足时，主 Agent必须停止进入章节正文。
- 应明确报告阻塞原因，不得静默改成主 Agent 单独完成。
- 工具恢复后，从“实际创建子 Agent”这一步继续。
- 最终交付必须报告实际使用的子 Agent 及完成范围。
- 若满足触发条件却最终无法提供子 Agent 标识，流程视为未通过，不得标记完成。

## 0.5 写作规范强制路由

只要任务要生成或实质修改学习笔记，开始正文前必须先读取：

`.agent-guides/通用写作与格式规范.md`

再按产物类型继续读取：

- `知识点型笔记` → `.agent-guides/知识点型笔记规范.md`
- `整章串联讲解笔记 / 讲解版` → `.agent-guides/讲解型笔记规范.md`
- `章节思维导图` → `.agent-guides/思维导图规范.md`
- `碎片知识问答 / 原子笔记` → `.agent-guides/碎片问答与原子笔记规范.md`

“参考”“建议查看”不足以满足本规则；命中即属于**必须读取**。

## 0.6 多种任务叠加触发

例如“系统重写整门凸优化课程，并生成讲解版和导图”，必须同时触发：

1. 长篇学习工作流；
2. 子 Agent 强制门禁；
3. 通用写作与格式规范；
4. 讲解型笔记规范；
5. 思维导图规范；
6. 若同时修改知识点笔记，再读取知识点型规范。

不能因为已读取一个 guide，就跳过其他实际涉及的 guide。

## 0.7 论文类学习任务

- 论文研读与文献笔记任务可以使用子 Agent 协助原文核对、方法梳理或笔记整理，但普通论文不因此自动触发子 Agent 强制门禁。
- 普通期刊论文、会议论文和篇幅较短的技术报告，默认以“一篇文献对应一篇主笔记”的方式整理，尽量不要按论文章节拆成多篇笔记。
- 较长的硕士论文、博士论文或其他具有明显书稿规模的长篇文献，可以根据实际结构分章节整理；若同时满足 `0.1 长篇资料自动触发` 的条件，仍须遵守长篇学习工作流。
- 文献笔记的具体形式、字段和内容结构暂不在本文件中规定，待用户后续确定后再补充对应规范。

---

# 一、四类笔记的职责边界

## 1. 知识点型笔记

负责：定义、定理、性质、模型、公式、成立条件、必要推导、使用方法、直接解释、易错点。

目标：`完整、准确、可查、可独立阅读`

详细规则：`.agent-guides/知识点型笔记规范.md`

## 2. 整章串联讲解笔记

负责：本章为什么这样展开、知识点为什么前后相连、公式和模型为什么这样建立、方法如何选择、整章如何形成统一理解。

目标：`易懂、连续、有主线，但不堆砌`

详细规则：`.agent-guides/讲解型笔记规范.md`

## 3. 章节思维导图

负责：快速回顾、知识定位、核心关系索引、方法入口、模型对照、易混关系。

目标：`高密度、短、快、可扫读`

思维导图不是第二篇讲解笔记。

详细规则：`.agent-guides/思维导图规范.md`

## 4. 碎片知识问答

负责：一个具体卡点、局部公式、特殊情况、易混概念、常见误区或需要单独复习的问题。

目标：`一篇只解决一个明确问题`

详细规则：`.agent-guides/碎片问答与原子笔记规范.md`

## 5. 不靠重复追求“完整”

- 知识点型稳定承载定义、公式和完整知识边界。
- 讲解版只保留理解主线所需的关键步骤与因果关系。
- 思维导图只保留高密度结论、公式、对应关系和必要双链。
- 局部疑问优先拆成碎片问答。

判断标准：

- `知识点型`：能查明“是什么、什么时候成立、怎么用”。
- `讲解版`：能理解“为什么这样推进”。
- `思维导图`：适合快速扫读。
- `碎片问答`：只解决一个问题。

---

# 二、资料与事实来源

## 1. 原始资料优先

数学事实优先来自：

- 原始教材；
- 课程讲义；
- PPT；
- 论文。

不以聊天回答作为数学事实的最终依据，也不因已有旧笔记就默认其内容正确。

对话内容主要用于理解方式、问题意识、学习卡点、类比和表达优化；数学结论仍应回到原始资料核对。

## 2. Zotero Markdown 优先

如果 Zotero 中已有 mineru 附件/可读 Markdown，应优先读取，而不是重复解析 PDF。

若完成当前学习任务所必需的文献尚不在 Zotero 中，可以补充下载并落库，但必须遵守以下流程：

1. 先检索 Zotero，确认不存在可复用的条目或附件，避免重复收录。
2. 只补充支撑当前学习主线所必需的文献，不因顺带发现相关资料而无边界扩充文献库。
3. 将条目和原始附件保存到 Zotero 中与当前主题对应的分类位置，并保留完整、可核对的来源信息。
4. 使用 llm-for-zotero 将原始附件转录为 MinerU 附件；转录未完成或失败时必须明确标记，不得声称已经读取 MinerU 内容。
5. 按项目现有文献笔记模板，在对应的 Obsidian 文献目录创建笔记，并建立可追溯到 Zotero 条目、附件或 citekey 的关联。
6. 若下载、Zotero 写入、MinerU 转录或 Obsidian 笔记创建中的任一步骤受阻，应保留已完成状态并报告剩余步骤，不得用临时网页摘录冒充完整落库。

资料优先级原则上为：

1. `Zotero 教材 Markdown`
2. `Zotero 讲义 / PPT Markdown`
3. `Zotero 条目及附件信息`
4. `Zotero 原始 PDF / 原始附件`
5. 其他可靠原始来源
6. 对话内容和已有笔记

教材 Markdown 主要负责章节结构、定义、定理、公式、正文例题和完整知识边界。

讲义 / PPT Markdown 主要负责课堂重点、教材之外的补充、额外推导、课程强调内容、特殊例子和课程特定表达。

## 3. 开始新章节前确认真实结构

应优先从原文确定：

- 实际章节边界；
- 小节数量与标题；
- 定义、定理、公式；
- 例题和特殊说明；
- 讲义补充内容。

不要预设某章固定有多少节。

## 4. 教材与讲义差异

- 教材定义完整章节边界。
- 讲义省略教材小节，不代表可以删除该小节。
- 讲义新增内容应并入最相关的教材知识点。
- 符号、参数化、推导形式不同时，先判断是否只是表示差异；存在实质差异时再结合课程语境核对。

## 5. Markdown 转换异常

若出现公式残缺、矩阵错位、图表缺失、页序错误、乱码或章节结构异常，不得直接把异常内容写入笔记，应回到原始 PDF / 附件核对。

## 6. 不得虚构资料读取情况

没有实际读取 Zotero Markdown 时，不得写“已根据 Zotero 教材整理”“已阅读 Zotero 讲义”。

必须区分：

- 已实际读取 Zotero Markdown；
- 使用了其他等价原始来源；
- 尚未完成 Zotero 原文复核。

---

# 三、既有笔记与章节结构

## 1. 既有笔记主要参考形式，不默认内容正确

主要参考：

- 排版；
- 表达密度；
- Obsidian 结构；
- 双链方式。

用户明确表示“不满意旧笔记”时，应把原始教材 / 讲义当成全新资料重新整理，而不是继续以旧笔记作为内容来源。

修改前应实际读取相关文件，不根据文件名猜测内容和结构。

## 2. 教材决定完整边界

- 不预设章节节数。
- 先查教材目录、教材 Markdown、讲义 Markdown 和课程资料。
- 教材定义一级章节结构。
- 讲义新增内容并入最相关位置，不另造与教材冲突的章节编号。

---

# 四、通用写作与 Obsidian 格式

详细规则统一放在：

`.agent-guides/通用写作与格式规范.md`

任何生成或实质修改学习笔记的任务都必须读取该文件。

根文件只保留以下不可遗漏的通用要求：

- 公式后根据需要说明变量、条件和结论含义。
- 容易混淆的随机变量、观测量、优化变量、固定量、待估参数必须明确区分。
- 决定理解的步骤不以“显然”“容易得到”“可知”跳过。
- 每篇学习笔记必须保留合规 YAML；`created` 不因后续修改重置，实质修改更新 `updated`。
- 不使用 `\boxed{}`。
- 思维导图默认行内公式优先。
- 双链放在真正发生知识连接的位置，不在文末机械堆“关联”。

---

# 五、长篇学习工作流摘要

详细执行方法必须读取：

`.agent-guides/长篇学习工作流.md`

核心原则：

`先建立全书地图，但不提前概括全书 → 每次只聚焦一个章节深读 → 所有章节分别完成后，再统一研究跨章联系与双链`

阶段划分：

1. `全书结构扫描`：只建索引，不提前建立最终结论。
2. `分章隔离学习`：当前章节上下文原则上只围绕当前章。
3. `最小必要回查`：只定点回查理解当前章所需的前置知识。
4. `分章验收`：当前章闭环通过后再进入下一章。
5. `全书综合`：所有章节完成后才研究跨章主线、依赖与概念演化。
6. `双链补全`：跨章关系延迟统一审查，避免过早强行串联。
7. `全局审校`：检查遗漏、术语符号漂移、重复和跨章网络质量。

默认不因使用子 Agent 就盲目并行；章节有前后依赖时按教材自然顺序推进。

---

# 六、其他 Agent 接续工作

其他 Agent 接手时，应先读取：

1. 根目录 `AGENTS.md`；
2. 若命中长篇任务，读取 `.agent-guides/长篇学习工作流.md`；
3. `.agent-guides/通用写作与格式规范.md`；
4. 本次产物对应的专项 guide；
5. 当前章节直接相关的已有笔记；
6. 对应 Zotero 教材 / 讲义 Markdown；
7. 用户当前指定的基准、只读范围和修改边界。

处理新章节时，默认顺序：

`确认原始资料 → 确定章节自然组织方式 → 知识点型笔记 → 整章讲解版 → 思维导图 → 必要碎片问答`

不要先做导图再反推教材内容，否则容易遗漏。

---

# 七、修改后强制检查

## 1. 长篇任务门禁

若属于长篇资料，检查：

- 是否在进入章节正文前实际创建子 Agent；
- 是否记录子 Agent 名称、任务名或标识；
- 是否明确主 Agent 与章节 Agent 的职责边界；
- 是否读取 `.agent-guides/长篇学习工作流.md`；
- 是否按章节依赖顺序推进，而非盲目并行；
- 子 Agent 创建失败时是否停止正文并明确报告；
- 最终交付是否报告实际使用的子 Agent 及完成范围。

任一关键项缺失，不得把触发型任务标记为完成。

## 2. 资料检查

- 是否实际读取 Zotero 教材 / 讲义 Markdown；
- 是否存在可用 Markdown 却只依赖旧笔记；
- 是否误把讲义省略内容当成教材不存在；
- 是否存在公式乱码而未回原 PDF 核对。

## 3. 四类笔记职责

检查是否出现：

- 知识点型写成讲解稿；
- 讲解版复制知识点全文；
- 思维导图写成第二篇讲解版；
- 局部问题没有拆成碎片问答。

专项质量检查以对应 guide 为准；通用格式检查以 `通用写作与格式规范.md` 为准。

---

# 八、当前已验证基准

第七章`统计估计`形成了一组已验证的方法基准，但**只复用方法，不复制模板**。

- 知识点型：`凸优化/7 统计估计/7.1 参数分布估计.md`
  - 参考完整性、公式密度、条件、直接解释、讲义补充方式。
- 思维导图：`凸优化/7 统计估计/7.0 统计估计思维导图.md`
  - 参考短标题、行内公式、高密度、快速扫读、与讲解版分工。
- 讲解版：`凸优化/7 统计估计/7 统计估计（讲解版）.md`
  - 参考每节一个核心问题、统一原理先讲、后续模型只讲新增差异、减少重复总结、主干和补充分层。

任何基准章节被用户指定为只读时，只能参照，不得修改。

---

# 九、用户反馈与规则维护

## 1. 新经验写到正确层级

- `控制流 / 触发条件 / 工具门禁 / 资料路由` → 根 `AGENTS.md`
- `某类笔记具体怎么写` → 对应 `.agent-guides/`
- `跨所有笔记类型的格式与表达` → `.agent-guides/通用写作与格式规范.md`

不要再因为新增一条写作经验就继续膨胀根文件。

## 2. 已明确否定的倾向不得反复出现

包括：

- 思维导图写成长篇讲解；
- 思维导图套固定模板；
- 思维导图大量行间公式导致过长；
- 思维导图使用拖沓长标题；
- 讲解版内容虽然必要但无层级堆砌；
- 同一原理多次完整推导；
- 正文、节末、全章重复总结；
- 为防漏触发而在具体课程子目录复制通用 `AGENTS.md`。

## 3. 避免子目录重复 AGENTS.md

只有某个子目录存在**真正的领域专属控制规则**，且无法合理放进现有 guide 时，才考虑增加局部 `AGENTS.md`。

仅为了重复、强调或“保险”根文件已有的通用规则，不得新建子目录 `AGENTS.md`。

## 4. 不过度拆分 guide

优先按`任务 / 产物类型`拆分，不要细碎拆成公式、标题、双链、Callout 等十几个小文件。

当前规范集合：

- `.agent-guides/长篇学习工作流.md`
- `.agent-guides/通用写作与格式规范.md`
- `.agent-guides/知识点型笔记规范.md`
- `.agent-guides/讲解型笔记规范.md`
- `.agent-guides/思维导图规范.md`
- `.agent-guides/碎片问答与原子笔记规范.md`

专项 guide **不负责决定自己何时被读取**；这一判断始终由根 `AGENTS.md` 完成。

---

# 十、最终工作原则

四类笔记共同组成稳定知识系统：

- `知识点型`负责准确和完整；
- `讲解版`负责理解和串联；
- `思维导图`负责压缩和复习；
- `碎片问答`负责解决真实卡点。

资料层面：

`尽量回到 Zotero 中的教材 / 讲义 Markdown 原文`

组织层面：

`根据章节本身决定架构，而不是套模板`

长篇任务层面：

`先全书建图 → 强制章节子 Agent → 分章隔离深读 → 全部完成后再跨章综合`

表达层面：

`完整，但不重复；易懂，但不堆砌；高密度，但仍然清晰。`
