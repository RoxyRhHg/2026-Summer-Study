---
tags:
  - 暑假学习
  - 学习路径
  - 逆问题
  - Plug-and-Play
  - 深度学习
created: 2026-07-13
updated: 2026-09-04
---

# 2026 暑假学习路径

> [!important] 当前主线
> 凸优化与 FISTA 已作为前置完成。本轮只推进：`PnP-ADMM → PnP-FISTA → PyTorch 逆问题编程 → Deep Image Prior 复现`。在 DIP 最小复现通过前，不进入真实计算成像系统、DPIR 或其他扩展论文。

## 一条路线，而不是论文清单

```text
已完成前置：Stanford Convex Optimization + FISTA
    ↓
阶段 1：PnP-ADMM —— 理解 prox 如何被 denoiser 替换
    ↓
阶段 2：PnP-FISTA —— 在同一 forward model 上理解加速 PnP
    ↓
阶段 3：PyTorch 逆问题预备 —— 把 A、loss、autograd、optimizer 串起来
    ↓
阶段 4：Deep Image Prior —— 完成单图像去噪最小复现
    ↓
阶段 5：统一对照 —— FISTA / PnP-ADMM / PnP-FISTA / DIP
```

## 已完成前置：只复核，不继续扩展

- `凸优化`：需要时定点回查光滑项、Lipschitz 梯度、近端算子、变量分裂和收敛条件，不再顺序重读 Stanford 课程。
- `FISTA`：以 [[1 FISTA（讲解版）]] 和 [[Beck 等 - 2009 - A Fast Iterative Shrinkage-Thresholding Algorithm for Linear Inverse Problems|Beck 与 Teboulle 2009]] 为复核入口。
- `复核上限`：遇到具体卡点才回查，单次不超过半天；不能因为前置不够“完美”而推迟后续编码。

## 阶段 1：PnP-ADMM

### 核心问题

- 经典 ADMM 的 $x$、$v$、$u$ 三步各自解决什么问题？
- $\operatorname{prox}$ 被 denoiser 替换后，算法还在最小化一个显式目标吗？
- Chan 2017 的 `bounded denoiser`、`continuation` 和 `fixed-point convergence` 分别保证什么？
- 数据子问题如何随着 forward model $A$ 改变，denoiser 模块为什么可以保持不变？

### Zotero 原始资料

1. [[Boyd 等 - 2011 - Distributed Optimization and Statistical Learning via the Alternating Direction Method of Multipliers|Boyd 等 2011：ADMM 复核]]
2. [[Chan 等 - 2017 - Plug-and-Play ADMM for Image Restoration - Fixed Point Convergence and Applications|Chan 等 2017：PnP-ADMM 主论文]]

两篇均已在 Zotero 的`2026暑假学习`分类中，并有 LLM for Zotero 的 MinerU 附件。主入口使用 [[0 PnP-ADMM（讲解版）]]；[[0.0 PnP-ADMM思维导图]]只用于复习，不再把论文拆成逐节笔记。

### 学习顺序

- `第 0 步：补桥`：先读 [[为什么从FISTA走向ADMM]]，明确 FISTA 与 ADMM 不是前后升级关系，而是同一 $f+g$ 问题的两种不同分裂框架；把 `variable splitting → augmented Lagrangian → alternating updates → scaled form` 这条链补齐。
- `第 1 步：半天复核`：从 Boyd 2011 定点回查 variable splitting、scaled form、primal residual 和 dual residual。
- `第 2 步：通读主论文`：先只标出问题、三步迭代、假设、定理和实验任务，不边读边拆笔记。
- `第 3 步：第二遍精读`：集中处理 bounded denoiser、continuation 和 fixed point；明确它们不自动等价于某个显式凸目标的最优解。
- `第 4 步：最小编码`：固定一个简单线性 forward model 和一个简单 denoiser，实现 PnP-ADMM，记录 $\rho$、等效噪声水平、残差和重建指标。

### 阶段出口

- 能不看资料写出 PnP-ADMM 三步迭代，并解释每个变量和参数。
- 能区分`经典 ADMM 的目标最优性`与`PnP 的不动点收敛`。
- 有一个可运行的最小实验，保存观测图、重建图、PSNR/SSIM、primal/dual residual 曲线和参数表。
- 未达到出口前，不读 DPIR，也不更换到真实光学 forward model。

## 阶段 2：PnP-FISTA

### 核心问题

- FISTA 中哪一步被 denoiser 替换，哪些动量结构保留？
- 线性 denoiser 在什么加权几何下可解释为 scaled proximal map？
- 目标值的 $O(1/k^2)$ 与迭代点的线性收敛为什么不是同一个结论？
- 在同一 $A$ 和 denoiser 下，PnP-FISTA 与 PnP-ADMM 的计算代价、参数和稳定性如何比较？

### Zotero 原始资料

1. [[Beck 等 - 2009 - A Fast Iterative Shrinkage-Thresholding Algorithm for Linear Inverse Problems|Beck 与 Teboulle 2009：FISTA 基线]]
2. [[Gavaskar 等 - 2021 - On Plug-and-Play Regularization using Linear Denoisers|Gavaskar 等 2021：线性 denoiser 与 scaled proximal map]]
3. [[Sinha 等 - 2024 - FISTA Iterates Converge Linearly for Denoiser-Driven Regularization|Sinha 与 Chaudhury 2024：PnP-FISTA 迭代收敛]]

三篇均已有 Zotero 条目、PDF 和 MinerU Markdown。主入口使用 [[0 PnP-FISTA（讲解版）]]；[[0.0 PnP-FISTA思维导图]]只承担快速复习。

### 学习顺序

- `第 1 步：复核 FISTA`：只回查迭代式、外推点和步长条件。
- `第 2 步：Gavaskar 2021`：理解线性 denoiser、对称性、加权内积和 scaled proximal 解释。
- `第 3 步：Sinha 2024`：把线性逆问题写成状态迭代，用谱半径理解迭代点的线性收敛。
- `第 4 步：公平比较`：复用阶段 1 的 $A$、观测数据和 denoiser，只替换求解器；画 objective/残差、$\lVert x_{k+1}-x_k\rVert$ 和指标曲线。

### 阶段出口

- 能从 FISTA 明确指出 PnP 替换发生的位置。
- 能解释普通欧氏 proximal 与 scaled proximal 的差异。
- 能分别陈述 Gavaskar 2021 和 Sinha 2024 的结论边界。
- 完成同一问题下 PnP-ADMM 与 PnP-FISTA 的可复现实验对照。

## 阶段 3：PyTorch 逆问题编程预备

### 只学 DIP 复现马上要用的内容

- `Tensor 与 device`：shape、dtype、batch/channel 维度、CPU/GPU 转移。
- `autograd`：叶子张量、计算图、`loss.backward()`、梯度清零和 `no_grad`。
- `nn.Module`：卷积、上采样、激活、skip connection，能读懂生成器的 forward。
- `optimizer`：Adam 的参数组、学习率、`zero_grad → forward → loss → backward → step`。
- `可微 forward model`：把 $y=Ax+n$ 中的 $A$ 写成 PyTorch 运算，并通过数据一致性 loss 反向传播。
- `实验纪律`：固定 seed，保存配置、best/last 输出、曲线和运行环境。

### Zotero 原始资料

- [[Paszke 等 - 2019 - PyTorch - An Imperative Style, High-Performance Deep Learning Library|Paszke 等 2019：PyTorch 论文]]，citekey：`paszkePyTorchImperativeStyle2019`。
- [[Ulyanov 等 - 2018 - Deep Image Prior|Ulyanov 等 2018：DIP 主论文]]中的方法与实现细节。

> [!warning] 来源状态
> PyTorch 论文条目已补入 Zotero 的`2026暑假学习`分类；PDF 与 MinerU 缓存仍需在 Zotero 中完成附件导入和解析。在该状态补齐前，不把网络上的零散教程写成正式学习笔记。

### 三个递进练习

- `练习 1`：优化一个张量，使 $\lVert Ax-y\rVert_2^2$ 下降，确认 autograd 和 forward model 正确。
- `练习 2`：把优化变量换成小型 `nn.Module` 的参数，固定输入 $z$，观察网络输出随迭代变化。
- `练习 3`：加入卷积、上采样和 skip connection，保存 best/last 图像与 loss 曲线，为 DIP 代码做结构预演。

### 阶段出口

- 能独立写出一个不依赖训练集的单图像优化循环。
- 能检查梯度是否存在、shape 是否正确、loss 是否真正由 $A(f_\theta(z))$ 计算。
- 三个练习都能从空环境重新运行，且结果目录中包含配置和曲线。

## 阶段 4：Deep Image Prior 最小复现

### 阅读与笔记边界

- 只保留一篇主讲解 [[0 Deep Image Prior（讲解版）]] 和一篇实现清单 [[4 DIP论文级复现清单与实验边界]]。
- 论文不再按章节拆成多篇笔记；局部卡点直接回到 Zotero MinerU 原文定位。
- 第一轮只复现`去噪`。超分辨率与 inpainting 是第二个任务，不与去噪并行。

### 最小复现顺序

- `第 1 步：数据`：选一张图像，固定噪声生成方式、噪声水平、seed 和评价指标。
- `第 2 步：模型`：固定随机输入 $z$，记录网络深度、通道数、上采样方式、skip connection 和参数量。
- `第 3 步：优化`：只优化 $\theta$，loss 使用观测数据一致性；保存每隔固定迭代的输出和滑动平均结果。
- `第 4 步：早停`：同时保存`best`和`last`，画 PSNR/SSIM 与迭代次数曲线，展示先拟合结构、后拟合噪声的过程。
- `第 5 步：最小消融`：只比较一个变量，例如有/无输入扰动或两种网络结构；一次只改一个因素。

### 阶段出口

- 结果能从配置和 seed 复现，不只留下 notebook 的最后一张图。
- 有 noisy / best / last / ground truth 对照，且能解释 early stopping 为什么是方法的一部分。
- 能说明 DIP 优化的是网络参数 $\theta$，而 FISTA/PnP 的主要迭代变量是图像 $x$。
- 完成后才允许进入超分辨率、inpainting 或 DPIR。

## 阶段 5：统一对照实验

复用同一张图像、同一观测模型和同一评价脚本，形成一张最小对照表：

| 方法 | 先验所在位置 | 主要迭代变量 | 必须记录 |
| --- | --- | --- | --- |
| FISTA | 显式正则项与 proximal operator | $x$ | 步长、正则参数、目标值 |
| PnP-ADMM | denoiser | $x,v,u$ | $\rho$、去噪强度、primal/dual residual |
| PnP-FISTA | denoiser | $x$ 与外推状态 | 步长、动量、迭代差、谱条件 |
| DIP | 网络结构与优化轨迹 | $\theta$ | 网络、seed、best/last、early stopping |

`统一对照完成`的判据不是某个方法赢，而是所有方法共享相同的数据、forward model、指标和结果记录格式。

## 五周执行节奏

| 周次 | 唯一主任务 | 本周必须交付 |
| --- | --- | --- |
| 第 1 周 | PnP-ADMM | 论文阅读卡 + 三步迭代 + 最小实验 |
| 第 2 周 | PnP-FISTA | 两篇理论阅读卡 + 与 ADMM 的公平对照 |
| 第 3 周 | PyTorch 逆问题预备 | 三个递进练习 + 可复现运行说明 |
| 第 4 周 | DIP 去噪 | 主复现 + best/last + early-stopping 曲线 |
| 第 5 周 | DIP 消融与统一对照 | 单变量消融 + 四类方法对照表 |

时间比例建议：`论文精读 30% / 编码 50% / 整理与复盘 20%`。如果当周代码没有跑通，不新增下一篇论文。

## 防发散规则

- `一阶段一出口`：未达到阶段出口，不开启下一阶段。
- `一篇论文一篇文献笔记`：不再把论文按章节拆成多篇知识点笔记。
- `同一实验只换一个因素`：先复用相同 $A$、数据和指标，再比较算法。
- `先合成问题，后真实系统`：DIP 最小复现前，不接单像素全息、光场、ToF、NLOS 等系统。
- `新增论文门槛`：只有当前主论文无法回答一个明确阻塞问题时，才向 Zotero 补文献。
- `每日收尾`：记录今天解决的问题、证据、代码结果和明天唯一下一步；不写泛泛的“继续学习”。

## 暂缓内容

以下内容已集中放入 [[99 暂缓资料]]，不是删除，而是暂时从主路线撤下：

- 计算成像前向模型；
- 单像素压缩全息；
- Compact Light Field Photography、ToF 与 NLOS；
- DPIR 与更深的 denoiser prior；
- 研究过渡和完整最小可复现实验体系；
- PnP/DIP 论文的旧版分章节笔记。

重新启用条件：`DIP 去噪最小复现通过 + 四类方法统一对照完成`。
